{
  "name": "sales-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run -w apps/backend start & npm run -w apps/frontend start",
    "build": "npm run -w apps/frontend build",
    "test": "npm run -w apps/backend test && npm run -w apps/frontend test -- --watchAll=false",
    "lint": "npm run -w apps/backend lint && npm run -w apps/frontend lint"
  }
}
