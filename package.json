{
  "name": "sales-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run -w apps/backend-nest start:dev & npm run -w apps/frontend dev",
    "dev:nest": "npm run -w apps/backend-nest start:dev",
    "dev:front": "npm run -w apps/frontend dev",
    "build": "npm run -w apps/frontend build && npm run -w apps/backend-nest build",
    "build:nest": "npm run -w apps/backend-nest build",
    "lint": "npm run -w apps/frontend lint && npm run -w apps/backend-nest lint"
  }
}
